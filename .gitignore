# Credenciales — nunca subir
.env
*.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Caches de trabajo: son megabytes de transcripciones que se pueden volver a
# generar, y no aportan nada al repositorio. OJO: corpus_reserva.json SI hace
# falta para --rellenar, pero es reconstruible con --rehacer-banco.
backend/scripts/corpus_candidatos_cache.json
backend/scripts/calibracion_supadata_cache.json
backend/scripts/enrich.log

# OS
.DS_Store
Thumbs.db

# La foto del dashboard: es el historial con titulos y canales, y docs/ es
# publico. El dashboard del sitio se lee en vivo; esta copia es para el anexo.
privado/

# Capturas que deja la prueba del sitio en el navegador
backend/tests/sitio/fotos/
