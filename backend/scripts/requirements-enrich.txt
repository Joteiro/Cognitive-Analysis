# Dependencias SOLO del enriquecimiento local (backend/scripts/enrich_local.py).
# Separadas de backend/requirements.txt a propósito: este script corre en tu
# máquina, no en Render, y Render no debe instalar yt-dlp ni intentar usarlo
# (YouTube bloquea las IPs de datacenter).
#
#   pip install -r requirements-enrich.txt

yt-dlp>=2025.1.1
youtube-transcript-api>=1.0.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.1
